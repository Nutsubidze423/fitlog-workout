export const metadata = { title: 'FitLog Workout' }
export default function RootLayout({ children }) {
  return <html lang="en"><body className="bg-slate-900 text-gray-100">{children}</body></html>
}
